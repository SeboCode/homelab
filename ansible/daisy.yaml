---
- name: Configure daisy node
  hosts: daisy
  roles:
    - mirror
    - base
    - firewall
    - firewall/dns
    - traefik
    - service/pihole
    - service/homeassistant
    - service/karakeep


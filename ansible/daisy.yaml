---
- name: Configure daisy node
  hosts: daisy
  roles:
    - sshd
    - mirror
    - base
    - tailscale
    - mount/daisy
    - firewall
    - traefik
    - service/pihole
    - service/homeassistant
    - service/karakeep
    - service/miniflux
    # - dhcp # TODO once isp fixes the closed dns port issue


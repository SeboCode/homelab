---
apiVersion: v1
kind: Service
metadata:
  name: server
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: server
  ports:
    - port: {{ .Values.server.port }}
      targetPort: {{ .Values.server.port }}

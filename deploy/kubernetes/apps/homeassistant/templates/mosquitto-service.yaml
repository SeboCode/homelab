---
apiVersion: v1
kind: Service
metadata:
  name: mosquitto
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: mosquitto
  ports:
    - port: {{ .Values.mosquitto.port }}
      targetPort: {{ .Values.mosquitto.port }}

---
# mosquitto.conf carries no secrets, unlike the password file it references.
apiVersion: v1
kind: ConfigMap
metadata:
  name: mosquitto-config-map
  namespace: {{ .Values.namespace }}
data:
  mosquitto.conf: |
    listener {{ .Values.mosquitto.port }}
    allow_anonymous false
    password_file /mosquitto/passwords/passwords

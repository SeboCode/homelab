---
apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app.kubernetes.io/name: traefik
  ports:
    - port: {{ .Values.dashboard.port }}
      targetPort: 8080

